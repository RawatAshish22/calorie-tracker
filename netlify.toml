[build]
  command = "npm run build"
  publish = "dist"

# Proxy API requests to the Render backend
[[redirects]]
  from = "/api/*"
  to = "https://calorie-tracker-w2p8.onrender.com/api/:splat"
  status = 200
  force = true
